# config.py

class Config:
    SECRET_KEY = 'sua_chave_secreta_aqui'  # Essencial para segurança do Flask
    # Outras configurações como banco de dados (se for usar), etc.
    # SQLALCHEMY_DATABASE_URI = 'sqlite:///site.db'
    DEBUG = True